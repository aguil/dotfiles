# Generated by `just proj::init <name>` from chezmoi — run recipes from this directory.
set shell := ["bash", "-eu", "-o", "pipefail", "-c"]

_TASKS_JUST := ___MODULE___
_PROJECT := ___PROJECT___

default:
  @DEV_PROJECT_NAME="{{ _PROJECT }}" DRY_RUN="${DRY_RUN:-}" just -f "{{ _TASKS_JUST }}" --list

new type task_id *repos:
  @DEV_PROJECT_NAME="{{ _PROJECT }}" DRY_RUN="${DRY_RUN:-}" just -f "{{ _TASKS_JUST }}" new "{{ type }}" "{{ task_id }}" {{ repos }}

add type task_id *repos:
  @DEV_PROJECT_NAME="{{ _PROJECT }}" DRY_RUN="${DRY_RUN:-}" just -f "{{ _TASKS_JUST }}" add "{{ type }}" "{{ task_id }}" {{ repos }}

list type="":
  @DEV_PROJECT_NAME="{{ _PROJECT }}" DRY_RUN="${DRY_RUN:-}" just -f "{{ _TASKS_JUST }}" list "{{ type }}"

status type="" task_id="":
  @DEV_PROJECT_NAME="{{ _PROJECT }}" DRY_RUN="${DRY_RUN:-}" just -f "{{ _TASKS_JUST }}" status "{{ type }}" "{{ task_id }}"

push type task_id *repos:
  @DEV_PROJECT_NAME="{{ _PROJECT }}" DRY_RUN="${DRY_RUN:-}" just -f "{{ _TASKS_JUST }}" push "{{ type }}" "{{ task_id }}" {{ repos }}

drop type="" task_id="" *repos:
  @DEV_PROJECT_NAME="{{ _PROJECT }}" DRY_RUN="${DRY_RUN:-}" just -f "{{ _TASKS_JUST }}" drop "{{ type }}" "{{ task_id }}" {{ repos }}
