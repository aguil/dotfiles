#!/usr/bin/env bash
set -euo pipefail

{{ if eq .chezmoi.os "windows" }}
exit 0
{{ else }}

hint_script="{{ .chezmoi.sourceDir }}/scripts/agents-sync-hint.sh"
if [[ ! -f "$hint_script" ]]; then
  exit 0
fi

bash "$hint_script" chezmoi-apply
{{ end }}
